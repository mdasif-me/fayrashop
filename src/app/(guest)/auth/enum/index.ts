export enum EStatus {
  VERIFIED = 'verified',
  UNVERIFIED = 'unverified',
  BANNED = 'banned',
}

export enum ERole {
  ADMIN = 'admin',
  MODERATOR = 'moderator',
  CUSTOMER = 'customer',
}

export enum EGender {
  MALE = 'male',
  FEMALE = 'female',
  OTHER = 'other',
  UNKNOWN = 'unknown',
}

export enum EAuthProvider {
  LOCAL = 'local',
  GOOGLE = 'google',
}
